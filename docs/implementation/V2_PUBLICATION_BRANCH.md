# CyFAST complete implementation publication branch

This branch is the authenticated GitHub publication path for the consolidated implementation. It preserves the legacy RabbitMQ agent and the first-party C# Windows Agent while adding the production real-Windows HTTP execution target and control-plane flow.
