NODE_ENV=local
DATABASE_TYPE_PRIMARY=mysql
DATABASE_TYPE_SECONDARY=mongodb
MESSAGING_TYPE=rabbitmq

AI_ENGINE_URL=http://127.0.0.1:8099

# LLM routes (requirements generate/regenerate, generation_validation, RAG chat): wait up to this many ms.
# Default in code is 600000 (10 min), above ai_engine LLM_HTTP_TIMEOUT_SECONDS (default 480s).
# AI_ENGINE_LLMS_TIMEOUT_MS=900000

#express server config
PORT=8086
HOST=localhost
HOST_URL=http://localhost:8086